plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace 'com.example.e_presensi'
    compileSdkVersion 36
    defaultConfig {
        applicationId "com.example.e_presensi"
        minSdkVersion flutter.minSdkVersion
        targetSdkVersion 36
        versionCode 1
        versionName "1.0"
    }
}

flutter {
    source '../..'
}
